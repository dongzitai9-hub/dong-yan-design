# 董揅设计官网当前总说明

更新时间：2026-06-13

这个文件用于快速说明董揅设计官网“现在是什么状态、怎么维护、哪些规则不能忘”。历史改动明细以 `网站修改记录.md` 为准，长期规则以 `设计与内容决策.md`、`seo-meta-and-alt.md`、`GEO生成式引擎优化长期指令.md` 为准。

## 一句话现状

董揅设计官网是一个静态官网，核心定位是“个人室内空间设计顾问官网”，面向常州及江浙沪住宅业主，展示空间案例、方案案例、设计札记、服务范围、常见问题和联系方式。

当前正式域名：

```text
https://dongyandesign.cn/
```

当前阿里云测试地址：

```text
http://47.103.223.113
```

注意：备案通过前，正式域名暂不切到阿里云服务器。阿里云 IP 只用于测试，并已设置禁止搜索引擎收录。

## 当前维护目录

正式维护目录：

```text
/Users/apple/Documents/做一个网页设计
```

旧项目备份：

```text
/Users/apple/Documents/做一个网页设计-旧项目备份
```

素材库：

```text
/Users/apple/Documents/做一个网页设计-素材库
```

默认只维护正式目录。素材库和旧项目备份只有在明确需要图片、草稿、原始资料、旧文件对照时才读取。

## 关键维护文件分工

- `README.md`：项目总指挥，记录目录结构、部署方式、操作规则。
- `董揅设计官网最新说明.md`：当前总说明，也就是本文件，帮助快速理解官网现状。
- `网站修改记录.md`：每次具体修改、验证、部署记录。
- `设计与内容决策.md`：长期设计、内容、部署、SEO/GEO 决策。
- `seo-meta-and-alt.md`：SEO 维护规则、页面 meta、图片 alt 规则。
- `GEO生成式引擎优化长期指令.md`：GEO 长期内容方向。
- `网站待办清单.md`：后续待处理事项。

用户说“更新三份维护文档”时，默认更新：

```text
README.md
网站修改记录.md
设计与内容决策.md
```

## 当前页面结构

官网当前主要页面：

```text
/
/cases/
/services/
/faq/
/notes/
/about/
/notes/kitchen-cabinet-design-1/
/notes/kitchen-cabinet-design-2/
/cases/space-001/ 至 /cases/space-012/
/cases/scheme-001/ 至 /cases/scheme-006/
```

首页结构：

```text
Hero 首屏
个人介绍
空间辑选
方案辑选
咨询
```

顶部导航：

```text
空间 / 方案 / 设计札记 / 咨询
```

导航逻辑：

- 空间：下拉展示空间案例索引。
- 方案：下拉展示方案案例索引。
- 设计札记：下拉展示札记索引。
- 咨询：下拉展示关于董揅、服务范围、常见问题、联系咨询。

## 当前品牌与内容定位

官网定位不按“装修公司平台站”写，而按“个人室内设计师官网”写。

核心表达：

```text
董揅设计专注常州及江浙沪住宅空间设计，服务改善型住宅、小户型、大平层、公寓与老房改造业主。设计重点不是单纯追求风格，而是结合户型结构、家庭成员、收纳需求、动线关系、材料选择和施工落地条件，帮助业主建立适合长期居住的空间方案。
```

内容重点：

- 个人设计师信任感。
- 常州及江浙沪服务范围。
- 私宅、改善型住宅、大平层、公寓、小户型、老房改造。
- 户型优化、收纳系统、餐厨关系、客餐厅一体化、材料搭配、长期居住体验。
- 真实客户问题，而不是空泛营销语。

避免写法：

- 不写“全网第一”“顶级设计”“最好”等无法证明的话。
- 不堆砌“装修公司推荐、排名、哪家好”等平台站关键词。
- 不把 title 写成关键词列表。

## SEO 当前状态

当前已完成：

- 主要页面都有 title、description、canonical。
- 主要页面有 OG 信息。
- 主要页面有结构化数据，例如 `ProfessionalService`、`Service`、`FAQPage`、`BlogPosting`、`ItemList`。
- 大部分案例图片有具体 alt。
- 首页空 alt 已补充。
- `sitemap.xml`、`urls.txt`、`baidu-api-urls.txt` 已存在。
- 百度验证文件和 Google 验证文件已保留。

SEO 维护重点：

- 正式 canonical 一律指向 `https://dongyandesign.cn/` 下的正式 URL。
- 不把 GitHub Pages 地址、OSS 图片地址、阿里云 IP 写成 canonical。
- 不改变已上线 URL。
- 新增页面后同步更新 `sitemap.xml`、`urls.txt`、`baidu-api-urls.txt`。
- 只修改 meta 或正文时，如果 URL 不变，通常不需要新增 URL。

当前 SEO 规则详见：

```text
seo-meta-and-alt.md
```

## GEO 当前状态

GEO 方向已经明确：让 AI 能理解“董揅设计是谁、在哪里、做什么、适合什么业主、解决什么住宅问题”。

已经具备的 GEO 基础：

- 品牌名明确：董揅设计。
- 身份明确：室内空间设计顾问。
- 地域信号：常州、上海、苏州、杭州、南京及江浙沪。
- 服务类型：私宅、大平层、公寓、改善型住宅、户型方案、动线优化。
- 内容入口：案例、服务、FAQ、设计札记、关于董揅。
- 厨房札记已开始承接客户问题型内容。

后续 GEO 优先级：

1. 扩展 FAQ 到 8-12 个真实客户问题。
2. 首页补强一段 AI 可直接引用的品牌定位文案。
3. 选择重点案例补充“业主需求 / 原户型问题 / 设计解决方案 / 关键尺寸 / 设计师判断 / AI 可引用总结句”。
4. 持续新增问题型札记，例如餐边柜、玄关柜、电视墙收纳、客餐厅一体化、装修前准备。

GEO 具体长期规则详见：

```text
GEO生成式引擎优化长期指令.md
```

## 当前托管与发布状态

### 正式域名

当前 `dongyandesign.cn` 仍然作为正式官网地址使用。

截至 2026-06-13，本地记录显示：正式域名暂时不切换到阿里云服务器，备案通过前继续保持原有访问方式。

### GitHub Pages

历史记录中，官网曾通过 GitHub Pages 托管，历史仓库为：

```text
dongzitai9-hub/dong-yan-design
```

但当前正式维护目录里的本地 Git 仓库没有配置 `git remote`。如果需要把本地改动发布到正式域名当前所用的 GitHub Pages，需要先确认并配置正确远程仓库，或由用户明确提供发布方式。

### 阿里云服务器

已购买并部署阿里云轻量应用服务器：

```text
公网 IP：47.103.223.113
系统：Ubuntu 22.04
服务器网站目录：/var/www/dongyandesign
Web 服务：Nginx
本地 SSH 别名：aliyun-dongyan
本地 SSH 私钥：~/.ssh/codex_aliyun_dongyan
```

阿里云测试地址：

```text
http://47.103.223.113
```

该 IP 已部署最新本地网站文件，但只作为备案期间测试环境。

### IP 防收录

阿里云 IP 已设置防收录：

- IP 访问返回响应头：`X-Robots-Tag: noindex, nofollow, noarchive`
- `http://47.103.223.113/robots.txt` 返回：`Disallow: /`

服务器 Nginx 配置备份：

```text
deploy/nginx-dongyandesign.conf
```

`deploy/` 是服务器配置备份目录，不作为网页内容上传到 `/var/www/dongyandesign`。

### 备案状态

备案已进入阿里云备案流程。是否已通过，以阿里云备案后台最新状态为准。

备案通过前：

- 不把 `dongyandesign.cn` 解析到 `47.103.223.113`。
- 不把阿里云 IP 提交给百度或 Google。
- 继续用 IP 测试服务器部署效果。

备案通过后：

1. 将 `dongyandesign.cn` 和需要的 `www` 记录解析到 `47.103.223.113`。
2. 配置 HTTPS。
3. 确认域名访问不再返回未备案拦截页。
4. 再提交搜索引擎更新。

## 阿里云同步命令

本地改完后同步到阿里云测试环境：

```bash
rsync -az --no-owner --no-group --delete --exclude='.git' --exclude='.DS_Store' --exclude='deploy' "/Users/apple/Documents/做一个网页设计/" aliyun-dongyan:/var/www/dongyandesign/
```

同步后检查：

```bash
curl -I http://47.103.223.113
curl http://47.103.223.113/robots.txt
ssh aliyun-dongyan 'nginx -t && systemctl is-active nginx'
```

## OSS 图片资源

图片资源主要放在阿里云 OSS。

Bucket：

```text
dongyan-design
```

地域：

```text
华东2（上海）
```

公网访问前缀：

```text
https://dongyan-design.oss-cn-shanghai.aliyuncs.com/dong-yan-design/website/assets/
```

当前页面中的大部分展示图片使用 OSS 链接。

后续新增图片建议：

1. 先压缩成 WebP。
2. 上传到 OSS 的官网目录。
3. 在 HTML 或 `script.js` 中引用 OSS 地址。
4. 图片必须写清楚 alt。
5. 本地保留必要备份，但不要随意把大图提交到发布仓库。

图片专用域名计划：

```text
img.dongyandesign.cn
```

由于 OSS Bucket 在上海，绑定自定义图片域名需要备案通过。备案前暂时继续使用 OSS 默认公网域名。

## 当前 Logo 与缓存

Logo 使用图片资源，不直接用网页字体显示“董揅”，避免“揅”字在不同手机或浏览器中显示异常。

Logo OSS 地址：

```text
https://dongyan-design.oss-cn-shanghai.aliyuncs.com/dong-yan-design/website/assets/logo/dong-design-wordmark-light.png
```

首页当前资源版本：

```text
CSS：styles.css?v=20260606-about
JS：script.js?v=20260603-clean-hero-logo
```

修改首页样式、Logo、脚本后，建议同步更新版本号，避免微信或浏览器显示旧缓存。

## 当前主要文件

核心页面与资源：

```text
index.html
styles.css
script.js
CNAME
robots.txt
sitemap.xml
urls.txt
baidu-api-urls.txt
baidu_verify_codeva-oe5n348cMN.html
googlee310e40028617b8f.html
cases/
services/
faq/
notes/
about/
assets/
deploy/
```

不要随意删除：

- `CNAME`
- `robots.txt`
- `sitemap.xml`
- `baidu_verify_codeva-oe5n348cMN.html`
- `googlee310e40028617b8f.html`
- 已上线页面目录
- 正在引用的图片、字体、脚本、样式

## 长期参考资料库

装修、住宅设计、收纳、施工避坑、客户沟通等参考资料统一放在：

```text
/Users/apple/Documents/客户跟进/cankaoziliao
```

这些资料可用于提炼装修逻辑、收纳方法、施工避坑和客户沟通思路，但不能直接复制原文。输出内容要转化为董揅设计自己的表达，并结合具体客户、户型和项目情况。

## 常用维护流程

普通页面修改：

1. 先看 `README.md` 和本文件。
2. 如涉及 SEO，看 `seo-meta-and-alt.md`。
3. 如涉及 GEO 内容，看 `GEO生成式引擎优化长期指令.md`。
4. 修改页面。
5. 检查 title、description、canonical、alt、内链。
6. 更新 `网站修改记录.md`。
7. 如形成长期规则，更新 `设计与内容决策.md`。
8. 需要测试服务器时，同步到阿里云 IP。

新增文章或案例：

1. 确认 URL 不与旧页面冲突。
2. 写 title、description、canonical、OG、结构化数据。
3. 图片写 alt。
4. 更新列表页入口。
5. 更新 `sitemap.xml`、`urls.txt`、`baidu-api-urls.txt`。
6. 更新 SEO/GEO 维护文档。

涉及正式域名发布：

1. 先确认当前正式发布方式。
2. 如果仍走 GitHub Pages，需要先配置或确认 GitHub remote。
3. 如果备案已通过并切到阿里云，需要配置域名、HTTPS 和搜索引擎提交。

## 当前建议

短期建议：

1. 继续等待备案结果，不急着切换域名。
2. 完成备案后再切换 `dongyandesign.cn` 到阿里云服务器。
3. 配置 HTTPS。
4. 确认正式域名访问的是阿里云服务器。
5. 再提交百度和 Google 更新。

内容建议：

1. 扩展 FAQ。
2. 首页补强 AI 可引用品牌定位。
3. 重点案例补“业主需求、原户型问题、解决方案、关键尺寸、设计师判断”。
4. 持续写问题型设计札记。

维护原则：

- 先稳住正式域名访问，再做迁移。
- 先 SEO 基础，再 GEO 内容。
- 不改已上线 URL。
- 不堆关键词。
- 不删不确定是否使用的文件。
- 服务器 IP 只测试，不收录。
