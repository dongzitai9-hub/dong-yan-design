# 网站资源体积梳理

日期：2026-06-20

## 当前体积

- `assets/`：约 181MB，已从迁出前约 362MB 减少约 181MB。
- `.git/`：约 573MB，仍属于历史体积问题，后续如需继续压缩需单独处理。
- `node_modules/`：约 17MB，属于本地检查依赖，不提交、不上线。

## 2026-06-20 迁出执行结果

- 已迁出未被正式页面引用的候选资源 131 个，约 181.0MB。
- 归档位置：`/Users/apple/Documents/做一个网页设计-素材库/网站资源归档/2026-06-20/`。
- 归档清单：`/Users/apple/Documents/做一个网页设计-素材库/网站资源归档/2026-06-20/MOVED_MANIFEST.md`。
- 迁出时保留原 `assets/` 下的目录结构，后续可按原路径恢复。
- 有 4 张图片仍被 `notes/kitchen-cabinet-design-1/index.html` 正式引用，未迁出：
  - `assets/notes/cabinet-design-real/long-countertop.png`
  - `assets/notes/cabinet-design-real/system-kitchen.png`
  - `assets/notes/cabinet-design-real/u-shape-layout.png`
  - `assets/notes/cabinet-design-real/u-vs-l-topview-real.png`

## 资源引用扫描结果

- 已扫描 `assets/` 中图片、视频、字体等媒体资源 844 个，合计约 360.1MB。
- 被正式页面、样式、脚本或维护清单直接引用的资源 356 个，约 128.7MB。
- 未发现直接引用的资源 488 个，约 231.4MB。

说明：未发现直接引用不等于可以直接删除，只能作为“迁出到素材库或归档”的候选。

## 优先迁出候选

这些目录或文件在 2026-06-20 已按“未被正式页面直接引用”的原则迁到素材库归档：

- `assets/photos/35/`：42 个文件，约 76.3MB。疑似早期 `space-002` 原图备份；当前正式页面未直接引用该目录。
- `assets/notes/cabinet-design-real/`：14 个文件，约 24.0MB。疑似早期橱柜文章图片版本。
- `assets/notes/cabinet-design/`：8 个文件，约 16.3MB。疑似早期橱柜文章图片版本。
- `assets/photos/05/`：14 个文件，约 9.3MB。
- `assets/photos/34/`：16 个文件，约 8.5MB。
- `assets/notes/kitchen-cabinet-design-v4/`：6 个文件，约 7.7MB。
- `assets/photos/29/`：10 个文件，约 6.2MB。
- `assets/photos/27/`：15 个文件，约 6.0MB。
- `assets/notes/kitchen-memory/`：2 个文件，约 5.4MB。

## 字体候选

当前 CSS 直接引用的是：

- `assets/fonts/ZhiMangXing.ttf`

以下字体未发现正式页面直接引用，可作为迁出候选：

- `assets/fonts/ZCOOLXiaoWei.ttf`：约 6.0MB。
- `assets/fonts/MaShanZheng.ttf`：约 5.6MB。
- `assets/fonts/LongCang.ttf`：约 4.9MB。
- `assets/fonts/LiuJianMaoCao.ttf`：约 4.7MB。

字体目录里仍保留当前 CSS 直接引用的 `ZhiMangXing.ttf`、标识字体和授权说明。已迁出的 4 个字体如需重新使用，可从素材库归档恢复。

## 暂不迁出的资源

- 首页视频 `assets/videos/generated/dongyan-design-consultation-loop-20260614.mp4` 约 12.3MB，当前首页直接引用 OSS 地址，本地可作为维护备份，是否迁出需看是否已经在素材库或 OSS 有完整备份。
- `assets/images/generated/editorial/` 多个首页视觉资源当前由首页引用 OSS 地址，本地可作为维护备份，暂不直接删。
- `assets/optimized/` 体积较小，且很多文件和旧 SEO/alt 清单有关，优先级低于大图和旧 notes 版本。

## `.git` 历史说明

- `.git/` 约 936MB，主要原因是图片和素材曾进入 Git 历史。
- 普通删除文件只能减小当前工作区和未来下载体积的一部分，不能自动减小历史仓库体积。
- 如果要真正压缩 Git 历史，需要单独做历史重写和强制推送。这会影响远端历史和协作，不能在未确认前执行。

## backup 远端整理

正式发布远端：

```text
origin  https://github.com/dongzitai9-hub/dong-yan-design.git
```

备份远端：

```text
backup  https://github.com/dongzitai9-hub/dongyan-design-site.git
```

`backup/main` 有独立备份历史，未被覆盖。为了保存当前正式站点状态，已在 backup 远端新增快照分支，并随正式 `main` 更新：

```text
formal-main-snapshot-2026-06-20
```

这样 backup 仓库同时保留原有备份主线和当前正式站点快照，不需要强推覆盖。

## 建议下一步

1. 迁出提交前运行 `npm run check:prepublish` 和 `npm run check:prepublish:render`。
2. 发布后线上抽查首页和核心页面。
3. `.git` 历史压缩单独处理，不和资源迁出混在一次操作里。
4. 后续如需恢复某个迁出资源，优先从素材库归档按原路径恢复，再运行发布检查。
