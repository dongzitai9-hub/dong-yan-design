# 设计与内容决策

用于记录已经确定的长期规则，避免后续反复改错。

## 已确定规则

- 正式维护目录固定为 `/Users/apple/Documents/做一个网页设计`。
- 旧项目完整备份固定为 `/Users/apple/Documents/做一个网页设计-旧项目备份`。
- 素材库固定为 `/Users/apple/Documents/做一个网页设计-素材库`。
- Codex 默认只读取正式维护目录；素材库和旧项目备份只在用户明确指定时读取。
- 不改变已上线页面 URL 路径，优先保护百度和 Google 已收录内容。
- 不随意修改 title、meta description、canonical、robots、sitemap。
- 不删除百度验证文件和 Google 验证文件。
- 不确定某个图片、字体、页面或文件是否正在使用时，先保留。
- `GEO生成式引擎优化长期指令.md` 负责长期 SEO/GEO/AI 搜索原则。
- `seo-meta-and-alt.md` 只在具体 SEO 执行任务中读取，例如修改 title、description、canonical、图片 alt、新增案例页、新增 notes 文章或更新 sitemap。
- 每次修改网站后，同步更新 `网站修改记录.md`；有后续任务时更新 `网站待办清单.md`；有长期规则时更新本文件。
- 用户说“更新三份维护文档”时，默认更新 `README.md`、`网站修改记录.md`、`设计与内容决策.md`。

## 2026-06-12 整理决策

- 正式网站目录保留所有已上线页面目录：`about/`、`cases/`、`services/`、`faq/`、`notes/`。
- 正式网站目录保留 `assets/`，因为其中包含网站页面和维护流程可能使用的图片、字体、标识、本地资源副本；不在本次整理中移动图片路径。
- `tmp/`、`drafts/`、`output/`、`wenanziliao/` 和 PPT 文件不进入正式网站目录，改放素材库。
- 正式网站目录不继承旧项目 `.git` 历史；当前目录已经初始化为干净 Git 仓库。

## 2026-06-12 路径决策

- 曾为避免旧对话提示“当前工作目录缺失”，短暂将正式网站项目本体从 `/Users/apple/Documents/做一个网页设计-正式网站` 移回 `/Users/apple/Documents/做一个网页设计`。
- 该路径安排已被后续“文件夹命名决策”取代，最终以后续记录为准。
- `/Users/apple/Documents/做一个网页设计-旧项目备份` 和 `/Users/apple/Documents/做一个网页设计-素材库` 始终保留在外部，不放入正式维护目录。

## 2026-06-12 文件夹命名决策

- 按用户要求，正式网站项目文件夹名称改为 `/Users/apple/Documents/做一个网页设计-正式网站`。
- 该文件夹命名安排已被后续“最终目录决策”取代，最终以后续记录为准。
- `/Users/apple/Documents/做一个网页设计-旧项目备份` 和 `/Users/apple/Documents/做一个网页设计-素材库` 继续保留在外部。

## 2026-06-12 最终目录决策

- 按用户要求，正式网站内容全部放入 `/Users/apple/Documents/做一个网页设计`。
- `/Users/apple/Documents/做一个网页设计` 现在就是正式网站项目本体，包含干净 Git 仓库。
- `/Users/apple/Documents/做一个网页设计-正式网站` 已删除，不再作为维护目录。
- `/Users/apple/Documents/做一个网页设计-旧项目备份` 和 `/Users/apple/Documents/做一个网页设计-素材库` 继续保留在外部。

## 2026-06-12 部署决策

- 阿里云轻量应用服务器作为备案通过后的正式服务器，公网 IP 为 `47.103.223.113`。
- 服务器端网站目录固定为 `/var/www/dongyandesign`。
- 服务器 Web 服务使用 Nginx，默认站点直接托管静态官网文件。
- 本地通过 SSH 别名 `aliyun-dongyan` 连接服务器，不再依赖明文密码登录。
- 备案审核期间，`dongyandesign.cn` 继续保持 GitHub Pages 访问，阿里云只用 `http://47.103.223.113` 做测试。
- 备案通过后，再把 `dongyandesign.cn` 和需要的 `www` 记录解析到 `47.103.223.113`，然后配置 HTTPS。
- 本地同步到阿里云服务器的默认命令为：

```bash
rsync -az --no-owner --no-group --delete --exclude='.git' --exclude='.DS_Store' --exclude='deploy' "/Users/apple/Documents/做一个网页设计/" aliyun-dongyan:/var/www/dongyandesign/
```

## 2026-06-12 维护表达决策

- 用户说“更新三份维护文档”时，不需要再追问，默认维护 `README.md`、`网站修改记录.md`、`设计与内容决策.md`。
- 用户说“按官网维护流程处理”时，默认包含修改网站、更新三份维护文档、检查效果；是否部署以用户当次要求为准。
- 每次涉及部署、备案、域名解析、HTTPS、服务器目录或 SSH 方式变化，都要同步记录到三份维护文档。
- 每次正式发布到 `https://dongyandesign.cn/` 后，无论改动大小，都必须更新 `README.md`、`设计与内容决策.md`、`网站修改记录.md` 三份固定维护文档。

## 2026-06-12 SEO 维护决策

- 董揅设计官网按“个人室内设计师官网”维护 SEO，不按平台站、装修公司目录站或泛家装流量站写法处理。
- SEO 核心顺序为：个人设计师信任感、服务城市、住宅类型、真实案例、客户问题、联系方式、技术标签。
- 重点服务词围绕常州及江浙沪住宅室内设计、私宅设计、改善型住宅、大平层、公寓空间、户型优化、收纳系统、动线优化、厨房收纳和装修前准备。
- 不堆砌“装修公司推荐、排名、最好、顶级”等无法证明的词，也不把 title 写成关键词列表。
- 栏目页 description 必须说明服务对象、空间类型、具体问题和咨询价值，不能只写一句泛介绍。
- 案例页不改 URL，优先补城市、面积、户型问题、解决方案、适合人群和专业判断。
- FAQ 和设计札记是后续 GEO/SEO 重点内容，应该持续回答客户真实会问的问题。
- 阿里云 IP 仅作备案期间测试，必须 noindex；正式 SEO 只围绕 `https://dongyandesign.cn/`。

## 2026-06-14 设计札记图片排版决策

- 设计札记正文以文字阅读宽度为基准，正文、单张图片、双图拼版、图文分栏和提示模块的最大宽度统一不超过正文文字宽度。
- 后续札记图片默认最多两张一排，不再使用三张一排；如果图片内文字较多，优先单张或两张排版，保证内容可读。
- 所有图片容器不得额外加外框线；如果图片自身模板里已有边框，那属于图片内容，不再通过 CSS 叠加边线。
- 手机端札记图片一律单列显示，必须检查无横向溢出。
- 新增札记发布前必须检查：`article img` 数量是否符合预期、`.poster-grid` 和 `.panel` 最大宽度是否不超过 `.article-body`、桌面端和手机端是否无横向滚动。

## 2026-06-14 设计札记预览流程决策

- 札记制作前期以本地预览和内容排版调整为主，不再每删一张图、改一段文案就同步维护记录。
- 预览阶段可先从页面中移除不需要的图片或文字；是否删除本地源文件、网站 WebP 和 OSS 远端对象，放到最终确认前统一整理。
- 用户说“最终确认”“准备发布”“可以发布”时，再统一执行：清理未引用图片、删除 OSS 多余对象、核对图片数量和 alt、检查桌面/手机无横向溢出、更新 `网站修改记录.md` 和 `seo-meta-and-alt.md`。
- 如果用户明确说“现在就删本地/OSS”，才在预览阶段立即删除素材和远端对象。

## 2026-06-15 媒体资源 OSS 默认规则

- 后续官网新增图片和视频时，默认上传到阿里云 OSS，不再等待用户单独提醒。
- HTML、CSS、`script.js` 中的图片和视频引用优先使用 OSS 公网地址：`https://dongyan-design.oss-cn-shanghai.aliyuncs.com/dong-yan-design/website/assets/`。
- 本地 `assets/` 可保留必要维护备份，但正式页面不再优先引用本地图片或视频路径。
- OSS 对象若暂时带有强制下载响应头，先不作为本次发布阻塞项；后续如需处理，单独到阿里云 OSS 控制台调整 bucket 预览/下载策略。

## 2026-06-14 日记与文章 GEO 编辑决策

- 后续用户写设计日记、设计札记或官网文章时，默认重点协助做 GEO 友好编辑，不只做普通润色。
- 编辑时要主动补强：客户会问的问题、设计师判断、适用/不适用条件、可执行标准、关键尺寸或数据、可被 AI 引用的总结句。
- 文章结构优先围绕真实客户问题展开，例如“适不适合”“怎么判断”“要注意什么”“尺寸多少合适”“什么家庭适合”。
- 不堆关键词，不写空泛营销语；关键词要自然放进标题、H1/H2、正文判断句、图片 alt、description 和 FAQ 中。
- 每篇文章发布前，尽量补 3-5 个简短 FAQ 或问答式段落，帮助搜索引擎和 AI 理解董揅设计对该问题的具体判断。

## 2026-06-14 资料提取与文章改写决策

- 后续从 PDF、书籍截图或参考资料写文章时，必须先做逐页内容提取，再进入文章编辑；不能只做空泛总结。
- 逐页提取要尽量保留每页的具体信息：核心观点、案例关系、空间问题、解决方法、尺寸数据、设计判断、图示含义和可转化为文章的表达点。
- 提取内容时仍然遵守不照搬原则：不复制原文段落，不使用 PDF 图片，不直接保留书中人物身份；人物统一改为“甲客户 / 乙客户 / 丙客户 / 某位业主”等匿名表达。
- 文章改写必须建立在逐页提取内容上，把资料里的具体信息重新组织成董揅设计自己的判断、语言和客户可读的问答式表达。
- 如果资料页数较多，先按页码列出提取摘要和可用素材，再决定文章结构、标题、FAQ、图片搭配和 GEO 表达。

## 2026-06-19 结构优化与发布检查决策

- 导航内容统一从 `assets/js/site-data.js` 读取，后续改导航入口、下拉内容、空间/方案/咨询链接时，优先改这个数据文件。
- 导航渲染统一由 `nav.js` 负责，不再在每个页面手工维护一份导航结构，降低局部页面被改漏或改坏的风险。
- 导航样式拆到 `assets/css/navigation.css`，后续导航颜色、下拉、手机端隐藏和滚动显示逻辑优先在这里维护。
- 空间和方案索引页建立 `assets/css/portfolio.css`，用于承接案例索引、更多辑选和索引页背景等局部样式。
- `styles.css` 暂时保留为历史全站样式文件，后续只做小步拆分，不能一次性大改，以免影响已经确认的页面。
- 正式发布前固定执行 `npm run check:prepublish` 和 `npm run check:prepublish:render`，检查首页、空间页、方案页、设计札记页、咨询页、一个空间详情页、一个方案详情页和 390px 手机端。
- 正式发布必须从完整克隆的 GitHub Pages 仓库操作，不从本地试验目录或脏目录直接推送。

## 2026-06-20 导航主页入口决策

- 顶部主导航固定保留“主页”入口，并放在“空间”左侧，顺序为：主页、空间、方案、设计札记、咨询。
- “主页”入口统一由 `nav.js` 在 `navGroups()` 中渲染，链接固定为 `/`；不要逐页手工复制一份导航结构。
- `assets/js/site-data.js` 的 `primary` 数据同步保留主页项，便于后续检查脚本或共享导航数据读取。
- 手机端为五个入口保留同排显示，导航间距和最大宽度优先在 `assets/css/navigation.css` 的移动端媒体查询中维护。
- 发布检查脚本必须把主导航顺序和“主页”入口点击返回首页纳入自动验证，避免后续结构调整时只检查到“导航存在”而漏掉入口顺序或链接行为。

## 2026-06-20 正式页面草稿路径拦截决策

- 正式 HTML 页面、结构化数据和可见链接里不能出现 `/drafts/` 路径。
- 案例详情页的面包屑结构化数据应指向正式栏目路径，例如空间案例统一指向 `https://dongyandesign.cn/cases/`。
- 发布检查脚本必须拦截 `dongyandesign.cn/drafts/` 和 `href="/drafts/`，避免旧草稿 URL 混入正式站点并影响搜索引擎理解。
- 发布检查脚本必须检查 `sitemap.xml` 的 URL 块结构、重复 URL 和本地页面存在性，避免站点地图损坏后仍被发布。

## 2026-06-20 资源整理决策

- 正式目录资源瘦身必须先做引用清单，再迁出候选资源；不能直接删除大图、字体、文章旧图或本地维护备份。
- 未被页面直接引用的资源只能先移到素材库或本地归档，并保留原目录结构，方便恢复。
- 资源迁出后必须运行 `npm run check:prepublish` 和 `npm run check:prepublish:render`，确认正式页面、导航、sitemap 和移动端渲染不受影响。
- `.git` 历史体积压缩属于单独高风险任务，不能和普通资源迁出混在一次操作里执行。
- backup 远端已有独立历史，不强推覆盖 `backup/main`；当前正式站点状态用 `formal-main-snapshot-2026-06-20` 分支保留。
