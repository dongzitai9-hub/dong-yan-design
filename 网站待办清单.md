# 网站待办清单

用于记录后续还需要处理或优化的事项。每次修改网站后，如发现新问题或新想法，补充到这里。

## 待办

- [ ] 后续如要进一步减小正式网站体积，可在确认 OSS 图片、上线引用和本地维护流程后，再细分 `assets/photos` 中的原图和备用图。
- [ ] 新增案例页或 notes 文章时，同步检查 `sitemap.xml`、`urls.txt`、`baidu-api-urls.txt`。
- [ ] 做 SEO/GEO 优化时，先读 `GEO生成式引擎优化长期指令.md`，再按需读 `seo-meta-and-alt.md`。
- [ ] 每次发布前检查 `robots.txt`、`sitemap.xml`、`CNAME`、百度验证文件、Google 验证文件是否仍在正式网站目录。
